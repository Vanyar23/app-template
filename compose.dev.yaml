services:
  app:
    build: .
    ports:
      - "127.0.0.1:__APP_PORT__:__APP_PORT__"
    environment:
      PORT: "__APP_PORT__"
      APP_NAME: "__APP_NAME__ (dev)"
    volumes:
      - ./src:/app/src:ro
